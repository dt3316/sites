git remote -v

git status

git add -A

git status

git commit -m 'README zengjia'

git push origin master

git pull origin master


在实际世界的学习和映射过程中。

必须先说明的问题：
1、所有状态和关键状态
2、关键过程
3、关键变量

从学习一个过程和方法的角度看，先要确定一个输入和一个输出，输入落到变量/对象，输出也落到对象。过程是通过方法来实现的。

每个状态作为一个对象：

从上一状态进入本状态的方法描述

